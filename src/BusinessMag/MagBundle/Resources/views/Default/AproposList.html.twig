{% extends 'MagBundle::base.html.twig' %}
{% block title %} A propos   {% endblock %}
{% block pagetitle %} <h3> A propos </h3> {% endblock %}
{% block pagenav %}
    <ul class="breadcrumb">
        <li class="breadcrumb-item"> Footer </li>
        <li class="breadcrumb-item">A propos</li>
        <li class="breadcrumb-item active"><a href="{{ path('apropos_list') }}">List</a></li>
    </ul>
{% endblock %}
{% block body %}



    <div class="col-lg-12">
        <div class="card">

            <div class="card-body">
                <div class="table-responsive">
                    <table class="table" id="table3">
                        <thead >
                        <tr>
                            <th> Qui Sommes-nous </th>
                            <th> Mentions légales </th>
                            <th> Champs CGV </th>
                            <th> Champs CGU </th>

                        </tr>
                        </thead>

                        {% for a in apropos %}
                            <tr>
                                <td> {{ a.quiSommesNous |striptags | slice(0, 20) ~ ' ...' }} </td>
                                <td> {{ a.mentionsLegales |striptags | slice(0, 20) ~ ' ...' }} </td>
                                <td> {{ a.champsCGV |striptags | slice(0, 20) ~ ' ...' }} </td>
                                <td> {{ a.champsCGU |striptags | slice(0, 20) ~ ' ...' }} </td>
                                <td> <a href="{{ path('apropos_edit', {'id' : a.id}) }}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a> </td>
                                {#<td><a href="{{ path('theme_delete', { 'id': t.id }) }}"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a></td>#}
                            </tr>
                        {% endfor %}

                    </table>
                </div>
            </div>
        </div>
    </div>

    </div>


{% endblock %}


