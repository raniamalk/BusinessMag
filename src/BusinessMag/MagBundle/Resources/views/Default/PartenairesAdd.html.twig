{% extends 'MagBundle::base.html.twig' %}
{% block title %} Partenaires {% endblock %}


{% block body %}

    <h3> Partenaires </h3>

    <div class="container">

        <form action="{{ path('partenaires_add') }}" {{ form_enctype(form) }} method="POST" >
            {{ form_widget(form) }}
            <br><br>

            {{ form_widget(form.Valider, {'attr': {'class': 'btn btn-primary'}}) }}
            {#<input class="btn btn-primary" type="submit" value="Enregistrer" />#}
        </form>

    </div>

{% endblock %}



{#{% block body %}
    <h3> Partenaires </h3>
        <div class="container">
            {{ form_start(form, {'attr':{'novalidate':'novalidate'}}) }}
            {{ form_widget(form) }}
            {{ form_widget(form.Valider, {'attr': {'class': 'btn btn-primary'}}) }}
            {{ form_end(form) }}
        </div>

{% endblock %}#}


