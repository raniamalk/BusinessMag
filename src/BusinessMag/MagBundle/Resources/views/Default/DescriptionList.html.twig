{% extends 'MagBundle::base.html.twig' %}
{% block title %} Texte Descriptif su site Business Mag {% endblock %}
{% block body %}
    <h3>Texte Descriptif su site Business Mag</h3>
    <div class="container">
        <table class="table table-bordered table-striped">
            <thead class="thead-dark">
                <tr>
                    <th> Description </th>
                    <th>Modification</th>
                    {#<th>suppression</th>#}
                </tr>
            </thead>

            {% for d in tdescri %}
                <tr>
                    <td> {{ d.description }} </td>
                    <td> <a href="{{ path('td_edit', {'id' : d.id}) }}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a> </td>
                    {#<td><a href="{{ path('theme_delete', { 'id': t.id }) }}"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a></td>#}
                </tr>
            {% endfor %}

        </table>

        {#<td><a href="{{ path('td_add') }}"><span class="btn info" aria-hidden="true"> Ajouter </span></a></td>#}
    </div>
{% endblock %}