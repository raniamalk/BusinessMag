{% extends 'MagBundle::base.html.twig' %}
{% block title %} Modification du champ A propos du site Business Mag {% endblock %}

{% block body %}
    <h3> Modifier le champ A propos du site Business Mag </h3>


    <form action="{{ path('apropos_edit',{'id': ids }) }}" {{ form_enctype(form) }} method="POST" >
        {{ form_widget(form) }}
        <br>
        {#<div class="clearfix">#}
            {{ form_widget(form.Valider, {'attr': {'class': 'btn btn-primary'}}) }}

            {#<a id="delbtn" class='btn btn-primary' role="button" href="{{ path('contenu_delete', { 'id': ids }) }}">Supprimer</a>#}
        {#</div>#}
    </form>

{% endblock %}

{% block javascript %}

    <script>
        CKEDITOR.replace( 'businessmag_magbundle_apropos[quiSommesNous]' );
    </script>
    <script>
        CKEDITOR.replace( 'businessmag_magbundle_apropos[mentionsLegales]' );
    </script>
    <script>
        CKEDITOR.replace( 'businessmag_magbundle_apropos[champsCGV]' );
    </script>
    <script>
        CKEDITOR.replace( 'businessmag_magbundle_apropos[champsCGU]' );
    </script>

{% endblock %}